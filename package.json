{
  "name": "glint-plugin-wrap-locale",
  "version": "1.0.0",
  "description": "adds `locale` function to the server and prefixes the containers id's with the `locale` + '-'",
  "main": "index.js",
  "browser": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/glintcms/glint-plugin-wrap-locale.git"
  },
  "author": "Andi Neck",
  "license": "MIT",
  "dependencies": {
    "debug": "^2.2.0"
  },
  "bugs": {
    "url": "https://github.com/glintcms/glint-plugin-wrap-locale/issues"
  },
  "homepage": "https://github.com/glintcms/glint-plugin-wrap-locale",
  "keywords": [
    "glint",
    "plugin",
    "wrap",
    "locale"
  ]
}
